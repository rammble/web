import { MainLayout } from 'src/layouts/MainLayout'
import { FC, PropsWithChildren } from 'react'

const Layout: FC<PropsWithChildren> = async ({ children }) => <>{children}</>

export default Layout
